# Figma 최종본 비즈니스 규칙 모음

## 1. 문서 범위

- 원본: [KTB 20th Figma](https://www.figma.com/design/a7gyhp6FvdTfmpNJNuofkd/KTB-20th-Figma?node-id=7223-21707&t=HwZxkvsQlKVBJTR0-1)
- 확인 기준: `최종본` 바로 아래의 V1, V2, V3. 최종 V3를 전체 기능의 기준으로 보고 V1·V2에서 이어지는 화면을 함께 확인했다.
- 제외 범위: 과거 버전 V1.1, 최종본 아래쪽의 별도 V2·V3, 컴포넌트 초안.
- 작성 목적: 화면 배치나 애니메이션 설명이 아니라, 서버·도메인 모델·정책 결정에 영향을 주는 조건, 권한, 상태 전이, 제한, 계산식, 중복 방지 및 실패 규칙을 모은다.

표기:

- **명시**: 기능 명세서에서 직접 확인되는 규칙
- **확인 필요**: 문구가 모호하거나 서로 다른 화면 간 정책을 추가로 합의해야 하는 규칙

## 2. 공통 규칙

### BR-COMMON-01 요청 중복 방지 — 명시

- 여행방 생성/참여, AI 일정 생성처럼 상태를 변경하는 요청은 처리 중 버튼을 비활성화한다.
- AI 일정 생성은 여러 클라이언트가 동시에 요청하더라도 하나의 생성 작업만 만들어야 한다.
- 이미 생성 중인 작업이 있으면 새 작업을 만들지 않고 기존 작업의 진행 상태를 보여준다.

### BR-COMMON-02 로딩과 실패 상태 — 명시

- 서버 검증이나 조회가 끝나기 전에는 다음 상태로 진행할 수 없다.
- 재시도 횟수가 정해진 작업은 한도를 넘으면 전용 실패 상태를 보여준다.
- 사용자에게 구분할 필요가 없는 내부 실패 원인은 서버 로그로만 남기고 사용자에게는 통합된 실패 메시지를 제공한다.

### BR-COMMON-03 탈퇴·나가기 사용자 집계 — 명시

- 여행방을 나간 사용자는 활성 멤버 목록과 설문 제출 통계에서 제외한다.
- 멤버 내보내기 기능은 제공하지 않는다.
- Figma에는 여행방 나가기 자체의 실행 화면과 최소 인원 미달 처리 정책은 명시되어 있지 않다.
- 별도 승인 정책에 따라 회원 탈퇴도 여행방 활성 멤버 감소와 동일하게 처리한다.

## 3. 로그인·초대

### BR-AUTH-01 초대 링크와 인증 — 명시

- 로그인 사용자가 유효한 초대 링크로 들어오면 해당 여행방 참여 화면으로 바로 이동한다.
- 비로그인 사용자는 초대 토큰을 보존한 채 로그인 화면으로 이동한다.
- 로그인 성공 후 메인이 아니라 원래의 여행방 참여 화면으로 복귀한다.
- 로그인 실패 또는 취소 시 로그인 화면에 머물며, 재시도 후 복귀할 수 있도록 초대 토큰을 유지한다.
- 보관한 초대 토큰은 로그인 흐름이 끝나거나 세션이 종료되면 폐기한다.

### BR-AUTH-02 초대 링크 유효성 — 명시

- 초대 토큰과 여행방 정보를 검증하는 동안 참여 버튼을 비활성화한다.
- 잘못된 초대 링크는 정상 참여 화면 대신 잘못된 링크 오류 화면으로 보낸다.
- 소프트 삭제된 여행방의 초대 링크는 삭제된 여행방 오류 화면으로 보낸다.
- 동일한 초대 링크는 링크가 유효한 동안 다시 열 수 있다.
- 여행이 시작됐거나, 설문 참여가 종료됐거나, 정원이 가득 찼거나, 이미 참여 중이면 초대 참여를 허용하지 않는다.
- 신규 멤버의 초대 참여는 `survey_deadline_at`이 지났거나 AI 일정 생성 작업이 시작되면 종료한다. 전원이 설문을 제출했다는 사실만으로는 종료하지 않는다.
- AI 생성 시작 전에 새 멤버가 참여하면 해당 멤버를 포함해 설문 진행률을 다시 계산한다.

## 4. 여행방

### BR-ROOM-01 여행 지역 — 명시

- 여행방은 하나의 광역 지역과 하나의 하위 지역을 선택한다.
- 서비스 지원 지역은 운영 전에 `regions` 테이블에 기준정보로 미리 등록한다.
- `regions`의 한 행은 광역·하위 지역 정보를 함께 가지며, 여행방의 `region_id`는 선택한 여행 지역 행의 PK인 `regions.id`를 참조한다.
- 광역·하위 지역의 코드와 이름은 모두 필수 기준정보로 관리한다.
- 지역 목록 API는 행들을 광역 지역별로 묶어 반환하지만 여행방 생성 요청은 최종 선택한 `regionId` 하나만 전달한다.
- 지역 기준정보가 비어 있으면 정상적인 빈 목록으로 처리하지 않고 여행방 생성 단계로 진행할 수 없는 서비스 오류를 표시한다.
- 광역 지역을 바꾸면 기존 하위 지역 선택을 해제한다.
- 선택한 하위 지역은 다시 눌러 해제할 수 있다.

### BR-ROOM-02 여행 날짜 — 명시

- 서울 날짜 기준 내일부터 여행 시작일을 선택할 수 있고 오늘과 과거 날짜는 선택할 수 없다.
- 여행 기간은 시작일과 종료일의 범위로 저장·표시한다.
- 시작일과 종료일을 포함해 1일부터 최대 10일까지 선택할 수 있다.
- 화면 표시 형식은 `YYYY.MM.DD - YYYY.MM.DD`이다.
- 지역과 날짜가 모두 선택되기 전에는 다음 단계로 진행할 수 없다.

### BR-ROOM-03 여행방 정원 — 명시

- 정원에는 방장을 포함한다.
- 기본 정원은 4명이다.
- 최소 정원은 2명, 최대 정원은 8명이다.
- 현재 인원이 최대 정원에 도달하면 추가 참여를 허용하지 않는다.
- 동시에 여러 사용자가 마지막 자리에 참여해 정원을 초과하면 초과된 참여 요청을 취소하고 오류를 반환한다.

### BR-ROOM-04 여행방 이름 — 명시

- 여행방 이름은 필수다.
- 공백만 있는 값은 허용하지 않는다.
- 최대 12자이며 초과 입력은 유효성 오류로 처리한다.
- 카드 등 제한된 영역에서는 초과 표시를 말줄임 처리한다.

### BR-ROOM-05 취향 조사 마감 — 명시

- 마감 선택지는 `1일 뒤`, `3일 뒤`, `여행 전날`, `직접 선택`이다.
- 기본값은 여행 전날이다.
- 마감 날짜는 서울 날짜 기준 오늘부터 여행 시작일 전날까지 선택할 수 있다.
- 마감 시간은 선택한 날짜의 `23:59:59.999999`로 고정하며 사용자는 시간을 따로 지정할 수 없다.
- `1일 뒤`나 `3일 뒤`가 여행 시작일 이상이면 해당 선택지를 사용할 수 없다.

### BR-ROOM-06 여행방 참여 — 명시

- 참여 확정 시 멤버십을 등록하고 해당 사용자의 취향 조사로 이동한다.
- 참여를 확정하지 않고 뒤로 가면 메인으로 돌아가며 멤버십은 만들지 않는다.
- 참여 화면에는 초대자, 여행 이름, 지역, 날짜, 현재 활성 인원을 표시한다.
- 초대 링크는 현재 방장만 조회·공유할 수 있다. 참여 화면의 초대자는 현재 방장을 뜻하며, 방장이 위임되면 기존 링크에도 새 방장을 표시한다.
- 참여자 프로필은 정해진 수까지만 보여주고 초과 인원은 `+N`으로 집계한다.

### BR-ROOM-07 참여자 및 설문 진행률 — 명시

- 참여자 목록에는 방장을 포함한 활성 멤버만 표시한다.
- 방장은 항상 맨 위, 나머지는 여행방 참여 순서로 정렬한다.
- 현재 사용자는 `(나)`로 구분한다.
- 동일 사용자를 중복 표시하지 않는다.
- 설문 진행률은 `제출한 활성 멤버 수 / 전체 활성 멤버 수 × 100`으로 계산한다.
- 마감 전 새 멤버가 들어오면 분모를 즉시 늘리고 진행률을 다시 계산한다.

### BR-ROOM-08 방장 권한 — 명시

- 최초 AI 여행 일정 생성과 날씨에 따른 일정 변경 결정은 방장만 할 수 있다.
- 일반 멤버는 방장의 일정 유지/변경 결정을 기다리며, 결정 결과는 모든 멤버 화면에 반영한다.

### BR-ROOM-09 나가기 — Figma 외 별도 정책 확정

- 멤버 내보내기 기능은 제공하지 않는다.
- Figma에는 나가기 실행 주체, 방장 위임, 최소 인원 미달, 마지막 멤버 퇴장, 초대 링크 폐기, 생성 중 작업 취소에 관한 규칙이 없다.
- 생성 직후 방장 1명만 있는 여행방은 초대를 위해 유지한다.
- 방장의 직접 여행방 삭제 기능은 제공하지 않는다. 서울 날짜 기준 여행 시작 전까지만 여행방 나가기를 허용하고 여행 중·후에는 나갈 수 없다.
- 여행 시작 전에 활성 멤버가 한 번이라도 2명 이상이 된 여행방은 나가기·회원 탈퇴로 1명만 남으면 소프트 삭제한다.
- 자동 삭제 시 마지막으로 남은 멤버십도 같은 시각에 `left_at`을 기록하고 비활성화하되 멤버십 이력은 물리 삭제하지 않는다.
- 회원 탈퇴는 여행 중·후에도 허용하지만 이로 인한 인원 감소로 진행 중·종료 여행방을 자동 삭제하지 않는다.
- 방장이 나가거나 탈퇴한 뒤에도 활성 멤버가 2명 이상 남으면 가장 먼저 참여한 활성 멤버에게 방장 권한을 이전한다.
- 소프트 삭제된 여행방의 초대 링크는 참여를 허용하지 않고 관련 오류 화면으로 연결한다.
- 진행 중인 AI 생성 작업은 취소하지 못하며, 작업이 완료되더라도 삭제된 여행방에는 결과를 반영하지 않는다.
- 여행 시작 전에 여행방이 삭제되면 일정과 하위 Day·장소·이동 구간을 애플리케이션에서 함께 정리한다. 여행방 소프트 삭제는 DB의 물리 삭제 cascade를 실행하지 않으므로 서비스 로직에서 처리한다.

## 5. 취향 조사

### BR-SURVEY-01 조사 단위와 제출 — 명시

- 취향 조사는 여행방에 참여하는 사용자별로 수행한다.
- 선호 항목은 5점 척도로 응답하며 처음 선택된 값은 중립인 `3`이다. 제출된 `1~5` 선택값은 별도 가공 없이 그대로 저장한다.
- 전체 문항 제출이 끝나면 해당 사용자의 조사 결과를 저장하고 여행방으로 이동한다.

### BR-SURVEY-02 희망 장소 — 명시

- 사용자는 선택한 여행 지역을 기준으로 Google Places 장소 검색 결과에서 희망 장소를 추가한다.
- 서버는 선택한 여행 지역의 광역 지역명을 사용자 검색어 앞에 보이지 않게 결합해 Google Places API를 호출한다. 좌표·행정구역 코드 사후 비교는 하지 않으므로 특정 행정구역 포함을 보장하는 하드 필터로 취급하지 않는다.
- Google Places 검색 결과 전체를 DB에 저장하지 않고 사용자가 결과를 클릭한 시점에 선택된 장소 하나만 `(regionId, googlePlaceId)` 기준으로 저장하거나 기존 장소를 조회해 내부 `placeId`를 반환한다. 별도 후보 Token은 사용하지 않는다.
- 검색 결과가 없으면 빈 결과 상태를 표시한다.
- 장소를 추가하면 검색 입력을 초기화한다.
- 추가한 장소는 카드로 표시하고 삭제하면 즉시 목록에서 제거한다.
- 사용자 한 명이 한 여행에서 선택할 수 있는 희망 장소는 최대 10개이며 선택하지 않아도 제출할 수 있다. 동일한 Google Places 장소는 중복 선택할 수 없다.

### BR-SURVEY-03 제출 결과 수정 — 명시

- 설문을 제출한 사용자는 자신의 기존 답변을 불러와 수정할 수 있다.
- 수정 완료 전까지는 이전 제출 결과가 유효하다.
- 수정 완료 시 새 결과가 이전 결과를 대체한다.
- AI 일정 생성 전 수정은 집계와 생성 입력에 반영한다.
- AI 일정 생성이 이미 진행 중이면 집계 화면은 갱신할 수 있으나 진행 중인 생성 요청의 입력은 바꾸지 않는다.
- AI 생성 시작은 기존 활성 멤버의 설문 재제출을 그 자체로 차단하지 않는다.
- 최초 제출은 `survey_deadline_at`까지 허용하고, 제출을 완료한 활성 멤버의 재제출은 설문 마감·AI 생성 상태와 관계없이 서울 날짜 기준 여행 시작 전까지 허용한다. 마감 전에 제출하지 않은 멤버는 마감 후 새로 제출할 수 없다.

### BR-SURVEY-04 집계 — 명시

- 집계에는 제출을 완료한 활성 멤버만 포함한다.
- 제출 완료된 활성 멤버의 원본 점수를 카테고리별로 산술 평균해 `1.0~5.0` 범위의 소수 첫째 자리로 표시한다. 0~100%로 환산하지 않는다.
- 멤버 간 응답 차이가 있을 때만 의견 차이 영역을 표시한다.
- 제외 선택은 장소가 아니라 `매운 음식`, `시끄러운 곳`, `술자리`, `장거리 도보`, `실내 활동`, `이른 아침 일정`과 같은 고정 카테고리다. 선택된 카테고리 코드는 AI 일정 생성 시 FastAPI 서버에 전달한다.
- 꼭 가고 싶은 장소는 Google Places 검색 후 `places`에 저장하고 AI 일정 생성 시 FastAPI 서버에 전달한다. 장소 추천과 포함·제외 판단은 FastAPI가 전담한다.
- 운영시간, 전체 여행시간 또는 이동거리 때문에 넣을 수 없다면 결과에 제외 이유를 설명한다.

## 6. AI 일정·경로 추천

### BR-ROUTE-01 최초 생성 가능 조건 — 명시

- 다음 중 하나를 만족해야 방장의 AI 일정 생성 버튼을 활성화한다.
  - 모든 활성 멤버가 설문을 제출했다.
  - 취향 조사 마감 시각이 지났다.
- 마감 후에는 현재까지 제출한 멤버의 결과만 사용한다.

### BR-ROUTE-02 생성 입력 — 명시

- 생성 요청에는 여행 지역, 여행 기간, 활성 참여자, 제출된 취향 조사 결과를 포함한다.
- FastAPI가 꼭 가고 싶은 장소, 제외 카테고리, 장소 운영 여부와 이동 가능성을 고려해 장소를 추천하고 일정을 구성한다. Spring 백엔드는 이 판단에 관여하지 않는다.

### BR-ROUTE-03 생성 작업 상태 — 명시

- 생성 진행은 `취향 모으기 → 후보 장소 찾기 → 최적 경로 계산`의 단계로 보여준다.
- 완료된 단계 다음 단계가 이어서 시작된다.
- 완료되면 생성된 여행 상세로 자동 이동한다.
- 새로고침이나 재진입 시 서버의 기존 작업 상태를 조회해 진행 화면을 복구한다.

### BR-ROUTE-04 여행 동선 — 명시

- 일정 장소는 방문 순서 번호를 가진다.
- 장소 사이의 방문 순서와 좌표 간 거리를 제공한다. 이동수단과 예상 소요 시간은 제공하지 않는다.
- 장소를 선택하면 해당 장소의 상세 정보를 제공한다.
- 일정 조회에 실패하면 정상 일정 대신 오류 상태를 제공한다.

### BR-ROUTE-05 날씨 기반 대체 일정 — 명시

- 여행 시작 하루 전 오전 10시에 여행방의 지역과 전체 여행 기간을 기준으로 날씨를 한 번 조회한다.
- 조회할 지역과 기간은 `weather_checks.trip_id`로 연결된 `trips`의 `region_id`, `start_date`, `end_date`와 `regions`를 조회해 결정한다.
- 여행 기간 전체에 특이 기상이 없으면 `weather_condition=NORMAL`, 비·폭우·폭염·한파·폭설·미세먼지 등이 확인되면 해당 대표 날씨 조건을 `weather_condition`에 저장한다.
- 특이 기상이 확인되면 방장에게 일정 유지 또는 변경 선택을 제공한다.
- 대체 일정은 기존 일정, 현재 여행 지역과 기간, 대표 날씨 조건, 그룹 취향, 장소 운영 여부 및 이동 가능성을 고려한다.
- 방장이 `REGENERATE`를 선택하면 FastAPI가 대체 일정을 생성한다. 생성 성공 시 별도의 두 번째 확인 없이 모든 멤버에게 보이는 현재 일정으로 자동 반영한다.
- 대체 일정 생성 실패 시 기존 일정은 유지하고 방장에게 실패 상태와 여행방 복귀를 제공한다.
- `KEEP`는 최종 결정이며 이후 변경할 수 없다. `REGENERATE`가 최종 실패하면 횟수를 차감하지 않고 여행 시작 전까지 다시 요청할 수 있다.
- 날씨 대체 작업은 동시에 하나만 실행하고, 성공 후에는 추가 날씨 재생성을 허용하지 않는다.
- 여행 시작 후에는 결정과 재시도를 허용하지 않는다. 시작 전에 실행된 작업은 취소하지 않지만 여행 시작 후 완료되면 새 일정을 저장·활성화하지 않고 작업을 `FAILED`, 내부 `WEATHER_REPLAN_EXPIRED`로 종료하며 기존 확정 일정을 유지한다.

## 7. 지역 오픈 채팅

### BR-CHAT-01 채팅방의 성격 — 명시 및 사용자 확인

- 오픈 채팅은 여행방 구성원 간 일정 조율 채팅이 아니다.
- 서비스를 이용하는 모든 사용자가 지역 정보를 얻고 공유하기 위해 참여하는 지역별 공개 채팅방이다.

### BR-CHAT-02 입장 동의 — 명시

- 지역 채팅방에 처음 들어갈 때 운영 정책을 조회해 표시한다.
- 동의 체크 전에는 입장할 수 없다.
- 동의 후 선택한 지역의 공개 채팅방으로 이동한다.

### BR-CHAT-03 채팅방 정렬 — 명시

- 사용자가 참여 중인 여행이 있으면 그 여행 지역의 채팅방을 목록 상단에 둔다.
- 나머지 채팅방은 현재 참여자 수가 많은 순으로 정렬한다.
- 참여자 수가 같으면 지역명 가나다순으로 정렬한다.

### BR-CHAT-04 메시지 제한 — 명시

- 빈 메시지는 전송할 수 없다.
- 동일 메시지의 반복 전송, 지나치게 빠른 전송, 욕설·모욕 등 금지 표현을 제한한다.
- 제한 사유에 맞는 경고를 사용자에게 제공한다.

### BR-CHAT-05 이미지 전송 — 명시

- JPEG, PNG와 WebP만 허용한다.
- 파일 크기는 최대 5MB다.
- 이미지를 선택하기 전에는 전송 버튼을 활성화하지 않는다.

### BR-CHAT-06 위반과 제재 — 명시

- 위반은 사용자 단위로 누적한다.
- 3회 미만은 경고 처리하고 이용 정지는 적용하지 않는다.
- 3회 이상부터 채팅 이용 정지를 적용하고 해당 채팅에서 퇴장시키며 남은 정지 시간을 안내한다.
- 반복 제재 기간은 `1일 → 4일 → 7일 → 14일 → 30일 → 60일`로 증가한다.
- 60일 단계 이후의 추가 제재도 최대 60일을 유지한다.
- 위반 3회마다 제재를 적용하고 다음 제재 판단을 위한 위반 묶음은 다시 0회부터 계산한다. 위반·제재 이력과 제재 단계는 유지한다.
- 정지는 사용자 전체에 적용하므로 모든 지역 채팅방 이용을 제한한다. 제재 종료 후 최신 운영 정책에 동의한 상태라면 다시 입장할 수 있다.
- 차단 메시지는 제재 근거로 저장하되 발신자 외 사용자에게 전송하거나 이력에 노출하지 않는다.
- 일반 공개 메시지는 90일 보관 후 삭제하고, 위반·제재에 연결된 차단 메시지는 제재 이력과 함께 보존한다.

## 8. 커뮤니티

### BR-COMMUNITY-01 게시 대상 — 명시

- 여행이 끝난 사용자의 여행 일정은 커뮤니티에 자동 업로드된다.
- 커뮤니티는 지역별 완료 여행 일정과 동선을 탐색하는 공간이다.
- 비로그인 사용자는 커뮤니티에 접근할 수 없다.
- 공개된 게시물과 일정 버전은 별도 삭제 정책 없이 계속 보관한다.

### BR-COMMUNITY-02 지역 필터 — 명시

- `전체`는 모든 지역을 조회한다.
- 특정 지역 선택 시 해당 지역 일정만 조회하고 필터 결과를 인기 기준으로 다시 정렬한다.

### BR-COMMUNITY-03 인기 점수 — 명시

- 인기 점수는 `(좋아요 × 3 + ln(조회수)) / (게시물 경과 시간 + 2)^1.2`로 계산하며 `ln`은 자연로그다.
- 점수를 고정값으로 저장하지 않고 최초 목록 요청의 `rankingAsOf`를 기준으로 조회 시 계산하여, 새로운 반응이 없는 게시물은 시간이 흐를수록 점수가 감소하게 한다.
- 인기 점수가 가장 높은 일정을 대표 인기 일정으로 선정한다.
- 메인 상단에는 전체 대표 인기 일정 하나를 표시하고 지역 필터에서는 해당 지역의 인기 일정을 제공한다.

### BR-COMMUNITY-04 좋아요와 조회 — 명시

- 사용자는 일정의 좋아요 수와 조회 수를 확인한다.
- 좋아요는 사용자가 다시 눌러 취소할 수 있다.
- 조회 수는 날짜가 바뀌어도 로그인 사용자와 게시물 조합당 전체 기간 한 번만 반영한다.

### BR-COMMUNITY-05 일정 상세 — 명시

- 일정 카드를 선택하면 상세 동선을 조회한다.
- 상세 동선은 방문 순서와 장소 간 좌표 거리를 제공하며 이동수단과 예상시간은 제공하지 않는다.
- 장소 선택 시 장소 상세 정보를 제공한다.
- 일정 로드 실패 시 오류 화면을 제공한다.

## 9. AI 포토 미션

### BR-MISSION-01 대표 여행 선택 — 명시

- 오늘이 여행 시작일과 종료일 사이에 있는 여행을 `진행 중 여행`으로 본다.
- 한 사용자는 날짜가 겹치는 여행에 참여할 수 없으므로 진행 중 여행은 최대 하나다.
- 진행 중 여행이 없으면 가장 가까운 미래에 시작하는 여행을 `가장 가까운 여행`으로 표시한다.
- 예정 여행도 없으면 해당 영역을 표시하지 않는다.

### BR-MISSION-02 지난 여행 — 명시

- 종료된 여행은 종료일이 최근인 순으로 정렬한다.
- 최초에는 가장 최근 여행 10개를 조회하고 마지막 항목에 도달하면 cursor로 다음 10개를 조회한다.
- 지난 여행이 없으면 지난 여행 목록을 표시하지 않는다.

### BR-MISSION-03 Day와 미션 유효기간 — 명시

- 여행 일수만큼 Day 탭을 만든다.
- 기본 선택은 오늘에 해당하는 Day다.
- 미래 Day는 비활성화하며 미리 열 수 없다.
- 미완료 미션은 여행 종료일까지 수행할 수 있다.

### BR-MISSION-04 일별 미션 생성 — 보류

- 미션 생성 시작 시점, 진행 단계, 내부 자동 시도 횟수와 사용자 수동 재요청 정책은 보류한다.
- 취향·해당 Day 여행 동선·날씨를 생성 입력으로 사용하고 동일 Day에 동시에 하나의 작업만 둔다는 데이터 모델만 유지한다.

### BR-MISSION-05 미션 범위와 완료 상태 — 명시

- 미션은 개인 미션과 그룹 미션으로 구분한다.
- 개인 미션 완료 상태는 사용자별로 관리한다.
- 그룹 미션은 멤버별 참여 현황과 완료 사진을 제공한다.
- 완료 수는 완료된 미션만 집계한다.
- 사진이 한 장도 없으면 미션 앨범 버튼을 비활성화한다.

### BR-MISSION-06 사진 제출 — 명시

- 카메라 촬영 또는 기기 앨범 선택으로 사진을 제출한다.
- 포토 미션 사진은 여행 시작 전에는 제출할 수 없다.
- 다시 제출하면 기존 제출 사진을 대체하되 재시도 횟수는 초기화하지 않는다.
- 여행 종료 전까지 지난 Day의 미션에도 사진을 제출할 수 있다.
- 본인이 제출한 사진만 삭제할 수 있다.

### BR-MISSION-07 사진 삭제 후 상태 — 명시

- 개인 미션 또는 그룹 미션의 마지막 남은 사진을 삭제하면 미션을 미완료로 되돌리고 앨범에서도 제거한다.
- 그룹 미션에 다른 사진이 남아 있으면 삭제한 사진만 앨범에서 제거하고 미션 완료 상태는 유지한다.
- 그룹 대표 사진을 삭제하면 남은 AI 성공 활성 사진 중 제출 시각이 가장 이른 사진을 대표로 승격한다.
- 삭제 후에도 AI 판정 재시도 횟수는 초기화하지 않는다.

### BR-MISSION-08 AI 사진 판정 — 명시

- AI 판정 결과는 일치도에 따라 3단계로 구분한다.
  - 80% 이상: 성공
  - 40% 이상 80% 미만: 유사/근접
  - 40% 미만: 실패
- 인식된 항목과 미션 조건의 일치 항목을 결과에 제공한다.
- 랜드마크 인식은 미션 판정과 독립적으로 수행하며 미션 성공 여부에 영향을 주지 않는다.
- 여러 랜드마크가 감지되면 신뢰도가 가장 높은 하나를 표시한다.

### BR-MISSION-09 판정 재시도와 수동 완료 — 명시

- AI가 사진을 알아보지 못한 경우 재촬영/재판정을 제공한다.
- AI 판정이 정상 완료되어 애매·실패 결과를 반환한 경우에만 사용자 재판정 횟수를 차감한다. 네트워크·외부 시스템·서버 오류로 결과를 만들지 못하면 차감하지 않는다.
- 최초 판정은 재판정 횟수에 포함하지 않고 이후 정상 완료된 재판정을 최대 3회 허용한다. 새 사진 제출과 기존 사진 삭제로 횟수를 초기화하지 않는다.
- 재판정 3회를 모두 사용해도 성공하지 못하면 본인의 참여에 `직접 완료 처리`를 제공한다.
- 직접 완료한 사진도 앨범에 저장하고 미션 완료 상태로 전환한다.
- 수동 완료 여부는 일반 AI 성공과 구분할 수 있도록 데이터로 남겨야 한다.

## 10. 알림

### BR-NOTI-01 읽음 상태 — 명시

- 로그인 사용자의 읽지 않은 알림이 하나라도 있으면 알림 아이콘에 표시점을 보여준다.
- 사용자가 알림을 선택하면 읽음으로 변경한다.
- `모두 읽음`은 현재 사용자의 모든 읽지 않은 알림을 읽음으로 바꾸고 아이콘 표시점을 제거한다.
- 이미 모두 읽은 상태라면 `모두 읽음` 동작을 제공하지 않는다.

### BR-NOTI-02 여행 전날 날씨 알림 — 명시

- 여행 시작 하루 전 오전 10시에 여행 지역과 전체 여행 기간의 날씨를 한 번 조회한다.
- `weather_condition=NORMAL`이면 기본 D-1 알림을 보내고, 특이 기상이면 해당 날씨 조건을 표시한 알림을 보낸다.
- 같은 여행과 사용자에게 동일 유형의 D-1 날씨 알림을 중복 생성하지 않는다.

### BR-NOTI-03 날씨 기반 일정 변경 알림 — 명시

- 일정 변경 판단이 필요한 기상 조건이면 방장에게 유지/변경 선택을 제공하고 멤버에게는 방장이 결정 중임을 알린다.
- 방장이 결정하면 모든 멤버 화면에 최신 상태를 반영한다.
- 날씨 조회에 실패하면 실패 로그를 남기고 참여자에게 날씨 조회 실패 알림을 제공한다.
- 날씨 조회 실패 시 날씨 기반 일정 변경 생성 알림은 보내지 않는다.

### BR-NOTI-04 알림 목록 — 명시

- 알림은 최신 생성 순으로 정렬한다.
- 알림 항목은 유형 아이콘, 제목, 간단한 설명, 생성 시각과 읽음 상태를 가진다.
- 최초 진입 시 20개를 조회하고, 마지막 항목에 도달하면 cursor로 다음 10개를 조회한다.
- 다음 페이지 조회 중에는 중복 요청을 막는다.
- 알림이 없으면 빈 상태를 표시한다.

### BR-NOTI-05 시간 표시 — 명시

- 1분 미만은 초 단위, 30분 미만은 분 단위로 표시한다.
- 30분 이상 1시간 미만은 10분 단위, 1시간 이상 24시간 미만은 시간 단위, 24시간 이상은 일 단위로 표시한다.

### BR-NOTI-06 이동 대상이 만료된 알림 — 명시

- 알림의 관련 일정이나 이동 대상이 더 이상 유효하지 않아도 알림 자체와 읽음 상태는 목록에 유지한다.
- 만료된 대상으로는 이동하거나 변경 작업을 다시 실행할 수 없다.
- 만료된 알림도 읽기 전까지 미읽음 수에 포함하고, 선택하면 읽음 처리 후 만료 안내를 표시한다.
- 알림은 생성 후 90일이 되면 `deleted_at`을 기록해 소프트 삭제하고 사용자 화면에서 제외한다. 물리 삭제 시점은 정하지 않는다.

## 11. Figma에서 정의되지 않은 핵심 정책

다음 항목은 Figma 화면 명세만으로 확정할 수 없었던 분석 당시의 목록이다. 이후 확정된 정책은 16절을 따르고, 현재도 미정인 항목은 18절만을 기준으로 판단한다.

1. 방 나가기의 권한, 방장 위임, 최소 유지 인원, 마지막 멤버 처리
2. 사용자의 여행 일정 겹침을 생성 단계에서 금지할지 여부
3. 대표 여행이 아닌 겹치는 여행의 포토 미션에 접근하는 방법
4. 여행 정보 변경 후 이미 생성된 AI 일정과 포토 미션을 폐기·재생성하는 조건
5. 커뮤니티 조회 수의 중복 방지 단위
6. 오픈 채팅 메시지 보관 기간, 신고·차단, 제재 해제 후 위반 횟수 초기화
7. 알림 보관 기간, 삭제 기능, 푸시 알림 동의 및 재발송 정책
8. AI 사진 판정에 사용되는 개인정보·이미지 보관 기간과 원본 삭제 정책

## 12. 구현 시 우선 보장할 불변식

1. 활성 여행방 인원은 정원을 초과하지 않는다.
2. 동일 여행방의 AI 일정 생성 작업은 동시에 하나만 존재한다.
3. AI 생성 입력은 요청 시작 시점의 설문 스냅샷으로 고정한다.
4. 나간 사용자는 활성 인원과 설문 집계에 포함하지 않는다.
5. 한 사용자의 활성 여행 날짜는 서로 겹치지 않으며 진행 중 여행은 최대 하나다.
6. 개인 사진은 제출자 본인만 삭제할 수 있다.
7. 동일 여행의 D-1 날씨 조회는 한 번만 생성하며, 동일 여행·사용자·알림 유형 이벤트는 중복 생성하지 않는다.
8. 오픈 채팅은 여행방과 무관한 지역별 공개 채널로 유지한다.

## 13. 후속 요구사항 정제 결과

이 장부터는 Figma 화면 명세를 직접 옮긴 내용이 아니라, Figma 분석 이후 문서 A와 문서 B를 비교하고 요구사항을 정제하면서 확정한 결과다. 따라서 앞 장의 `명시` 표기와 구분해서 사용해야 한다.

- 기준 문서: Notion `0단계: 프로젝트 요구사항 분석`(문서 B)
- 최종 정리일: 2026-09-01
- 정리 원칙:
  - 새로 정의한 7개 도메인만 사용한다.
  - 비즈니스 규칙, 기능 요구사항, 비기능 요구사항을 분리한다.
  - 의미가 같은 요구사항은 중복 추가하지 않는다.
  - 요구사항 본문은 보존하고 분류와 식별자를 정규화한다.
  - 근거가 없는 응답시간이나 동시 사용자 수 등의 수치는 임의로 확정하지 않는다.

## 14. 확정된 7개 도메인

| 순서 | 도메인 | 책임 | 요구사항 식별자 |
| --- | --- | --- | --- |
| 1 | 인증, 인가 | OAuth 로그인, 인증 상태, 로그아웃, 회원 상태와 접근 권한 | `BR-AUTH`, `AUTH` |
| 2 | 여행 | 여행방 생성·참여, 여행 정보, 정원, 초대와 멤버 관리 | `BR-TRIP`, `TRIP` |
| 3 | 설문 기반 일정 생성 | 취향 조사, 취향 집계, AI 후보 경로 생성, 확정 및 일정 수정 | `BR-SCHEDULE`, `SCHEDULE` |
| 4 | 오픈 채팅 | 지역별 공개 채팅, 정책 동의, 메시지·이미지 전송과 제재 | `BR-CHAT`, `CHAT` |
| 5 | 커뮤니티 | 완료 일정 공개, 지역 분류, 좋아요·조회수와 랭킹 | `BR-COMMUNITY`, `COMMUNITY` |
| 6 | 포토 미션 | Day 미션, 사진 제출·판정, 앨범, 랜드마크와 회고 | `BR-MISSION`, `MISSION` |
| 7 | 알림 | D-1 날씨 알림, 조회 실패 알림, 읽음 상태와 목록 | `BR-NOTI`, `NOTI` |

기존의 `여행 계획`, `취향 조사`, `경로 추천` 분류는 최종 도메인 이름으로 사용하지 않는다. 취향 조사와 AI 경로 추천은 `설문 기반 일정 생성`이 함께 책임진다. 날씨를 계기로 실행되더라도 일정 유지·변경과 대체 일정 생성은 알림 전달이 아니라 `설문 기반 일정 생성`의 책임으로 분류한다.

## 15. 요구사항 현황과 번호 체계

### 15.1 비즈니스 규칙

| 도메인 | 식별자 범위 | 개수 |
| --- | --- | ---: |
| 인증, 인가 | `BR-AUTH-001` ~ `BR-AUTH-010` | 10 |
| 여행 | `BR-TRIP-001` ~ `BR-TRIP-018` | 18 |
| 설문 기반 일정 생성 | `BR-SCHEDULE-001` ~ `BR-SCHEDULE-036` | 36 |
| 오픈 채팅 | `BR-CHAT-001` ~ `BR-CHAT-014` | 14 |
| 커뮤니티 | `BR-COMMUNITY-001` ~ `BR-COMMUNITY-007` | 7 |
| 포토 미션 | `BR-MISSION-001` ~ `BR-MISSION-026` | 26 |
| 알림 | `BR-NOTI-001` ~ `BR-NOTI-007` | 7 |
| **합계** |  | **118** |

### 15.2 기능 요구사항

| 도메인 | 식별자 범위 | 개수 |
| --- | --- | ---: |
| 인증, 인가 | `AUTH-001` ~ `AUTH-005` | 5 |
| 여행 | `TRIP-001` ~ `TRIP-005` | 5 |
| 설문 기반 일정 생성 | `SCHEDULE-001` ~ `SCHEDULE-018` | 18 |
| 오픈 채팅 | `CHAT-001` ~ `CHAT-008` | 8 |
| 커뮤니티 | `COMMUNITY-001` ~ `COMMUNITY-004` | 4 |
| 포토 미션 | `MISSION-001` ~ `MISSION-013` | 13 |
| 알림 | `NOTI-001` ~ `NOTI-005` | 5 |
| **합계** |  | **58** |

### 15.3 비기능 요구사항

| 분류 | 식별자 범위 | 개수 |
| --- | --- | ---: |
| 성능 | `NFR-PERF-001` ~ `NFR-PERF-004` | 4 |
| 장애 대응 | `NFR-INCIDENTRESPONSE-001` ~ `NFR-INCIDENTRESPONSE-003` | 3 |
| 데이터 정합성 | `NFR-CONSISTENCY-001` ~ `NFR-CONSISTENCY-006` | 6 |
| 보안 | `NFR-SECURITY-001` ~ `NFR-SECURITY-007` | 7 |
| 가용성 및 장애 대응 | `NFR-AVAILABILITY-001` ~ `NFR-AVAILABILITY-003` | 3 |
| 확장성 | `NFR-SCALABILITY-001` ~ `NFR-SCALABILITY-004` | 4 |
| 운영 및 모니터링 | `NFR-OBSERVABILITY-001` ~ `NFR-OBSERVABILITY-003` | 3 |
| **합계** |  | **30** |

모든 식별자는 각 분류에서 `001`부터 연속되며 중복되지 않는다.

## 16. Figma 분석 이후 구체화된 주요 정책

### 16.1 인증, 인가

- 카카오 OAuth 인증을 사용한다.
- 미가입 사용자는 OAuth 인증 완료 후 회원 정보를 만들고 로그인 처리한다.
- 기존 사용자는 별도 회원가입 없이 로그인한다.
- 유효한 인증 토큰으로 인증 상태를 유지하며 로그아웃 후에는 인증 기능 접근을 차단한다.
- 회원 탈퇴는 소프트 삭제로 처리한다.
- 회원 탈퇴 시 사용자가 등록한 포토 미션 사진 삭제와 관련 상태 갱신은 포토 미션 도메인이 담당한다.

### 16.2 여행

- 여행방 생성자는 최초 방장이 된다.
- 여행방 멤버 내보내기 기능은 제공하지 않는다.
- 여행 멤버는 서울 날짜 기준 여행 시작 전까지만 여행방 나가기 기능을 사용할 수 있다.
- 방장이 탈퇴하거나 방을 나간 뒤에도 활성 멤버가 2명 이상 남으면 가장 먼저 참여한 활성 멤버에게 방장 권한을 이전한다.
- 생성 직후 방장 1명만 있는 여행방은 초대를 위해 유지한다.
- 여행 시작 전에 활성 멤버가 한 번이라도 2명 이상이 된 여행방이 나가기·회원 탈퇴로 1명만 남으면 여행방을 소프트 삭제하고 마지막 멤버십도 종료한다.
- 방장의 직접 여행방 삭제 기능은 제공하지 않는다. 여행 중·후에는 나가기를 허용하지 않으며, 회원 탈퇴는 허용하되 이로 인해 여행방을 자동 삭제하지 않는다.
- 소프트 삭제된 여행방의 초대 링크는 참여할 수 없으며 관련 오류 화면을 표시한다.
- 나가기·회원 탈퇴로 여행방이 자동 삭제돼도 진행 중인 AI 생성 작업은 취소하지 않고 계속 실행하되, 완료 결과를 삭제된 여행방에 반영하지 않는다.
- 여행 시작 전 여행방이 자동 삭제되면 일정과 그 하위 Day·장소·이동 구간을 서비스 로직에서 함께 정리한다.
- 여행 정원은 방장을 포함해 2~8명이다.
- 동일 사용자는 같은 여행방에 중복 참여할 수 없다.
- 동시 참여 요청에서도 정원을 초과해 멤버가 등록되면 안 된다.
- 여행 시작일은 서울 날짜 기준 최소 내일이며, 시작일과 종료일을 포함해 당일 여행부터 최대 10일까지 허용한다.
- 사용자는 시작일과 종료일을 포함한 날짜 범위가 하루라도 겹치는 다른 여행방을 생성하거나 참여할 수 없다.
- 여행방 이름·지역·시작일·종료일·정원·설문 마감 시각은 생성 후 수정할 수 없으며 여행방 정보 수정 기능을 제공하지 않는다.
- 설문 재제출과 확정 일정의 장소 추가·삭제는 여행방 기본 정보 수정과 별개의 기능으로 유지한다.
- 잘못 생성한 여행방은 여행 시작 전에 구성원이 각자 나가 자동 삭제되게 한 뒤 다시 생성한다.
- 소프트 삭제된 여행방과 비활성 멤버십은 여행 날짜 중복 검사에서 제외한다.
- 동시 생성·참여 요청에서는 먼저 확정된 여행만 성공하고 나머지는 날짜 충돌로 거부한다.
- 초대 링크는 만료시키지 않되, 여행방이 가득 찬 경우 참여를 허용하지 않는다.
- 여행방마다 하나의 고정 초대 토큰 원문을 저장하고, 현재 방장이 다시 조회해 동일한 초대 링크를 공유할 수 있게 한다. 전체 URL은 저장하지 않는다.
- 잘못된 초대 링크와 삭제된 여행방의 초대 링크에는 각각 관련 오류 화면을 표시한다.
- 여행 시작, 설문 참여 종료, 정원 초과와 기존 참여 상태에서도 초대 참여를 허용하지 않는다.
- 신규 멤버의 초대 참여는 `survey_deadline_at`이 지났거나 AI 일정 생성 작업이 시작되면 종료하며, 전원이 설문을 제출했다는 사실만으로는 종료하지 않는다.
- AI 생성 시작 전에 새 멤버가 참여하면 새 활성 멤버를 포함해 설문 진행률을 다시 계산한다.

### 16.3 설문 기반 일정 생성

- 사용자와 여행의 조합마다 하나의 최종 설문 결과를 관리한다.
- 설문 수정이 완료되면 취향 집계를 다시 계산한다.
- 진행 중인 AI 생성 작업은 요청 시작 시점의 설문 스냅샷을 유지한다.
- AI 생성 중에도 기존 활성 멤버는 설문을 재제출할 수 있지만 해당 작업의 생성 결과는 바뀌지 않는다.
- 모든 멤버가 제출했거나 설문 마감 시각이 지나면 방장이 생성을 시작할 수 있다.
- 한 여행방에서 AI 일정 생성 작업은 동시에 하나만 존재한다.
- 동일 AI 작업의 일시적 오류 자동 재시도는 최초 스냅샷을 유지한다. 작업이 최종 실패한 뒤 방장이 다시 생성을 요청하면 최신 설문으로 새 작업과 새 스냅샷을 만들고 이전 실패 이력은 유지한다.
- 일정 확정 전 방장의 사용자 재생성은 성공 결과 기준 최대 3회까지 허용한다. 결과를 만들지 못한 실패, 내부 자동 재시도와 날씨 대체 일정은 횟수에 포함하지 않는다.
- 일정이 한 번 확정되면 일반 사용자 재생성을 허용하지 않는다. 확정 일정 장소 추가·삭제와 날씨 대체 일정은 별도 정책으로 유지한다.
- 새 생성이 성공하면 과거의 선택되지 않은 후보 일정은 삭제하되 작업과 입력 스냅샷은 이력으로 유지한다. 기존 확정 일정은 새 후보를 확정할 때까지 유지하고 이후 `SUPERSEDED`로 보존한다.
- 후보 경로는 `최단거리`, `취향 우선`, `균형 코스` 3개를 제공한다.
- 방장만 후보를 최종 일정으로 확정하고 확정 일정의 장소를 추가·삭제할 수 있다.
- 후보 재확정과 확정 일정의 장소 추가·삭제는 서울 시각 기준 여행 시작 전날까지 허용한다.
- 확정 일정의 장소는 원하는 Day의 처음, 두 장소 사이 또는 마지막에 추가하거나 삭제할 수 있다. 별도 순서 변경과 Day 간 장소 이동은 제공하지 않는다.
- AI 일정은 장소 좌표 간 거리의 합이 짧아지도록 방문 순서를 계산하며 이동수단과 소요 시간을 고려하지 않는다.
- AI 일정에서는 음식점끼리 또는 카페끼리 연속 배치하지 않고, `음식점 → 카페 → 음식점`과 `카페 → 음식점 → 카페`도 허용하지 않는다. 장소 대체와 생성 가능 여부는 FastAPI가 결정한다.
- 장소 추가·삭제 시 FastAPI나 AI가 방문 순서를 다시 구성하지 않는다. 백엔드는 변경된 인접 장소의 좌표 간 거리만 갱신하며 음식점·카페 배치 제약을 적용하지 않는다.
- 날씨 변경 요청에 방장이 응답하지 않으면 여행 시작 시 시스템이 기존 확정 일정을 유지한다.
- 일정에는 최소 3개, 최대 20개의 장소를 유지한다.
- 날씨 변경을 선택한 경우에만 FastAPI가 대체 일정을 생성하며, 성공하면 별도 재확인 없이 현재 일정으로 자동 반영하고 실패하면 기존 일정을 유지한다.
- 날씨에 따른 일정 유지·변경과 대체 일정 생성 책임도 이 도메인에 속한다.

### 16.4 오픈 채팅

- 행정구역별 공개 채팅으로 운영하며 여행방 내부 채팅으로 모델링하지 않는다.
- 정책 위반 횟수는 사용자별로 관리한다.
- 1·2회 위반은 메시지를 차단하고, 누적 3회에는 이용 정지를 적용한다.
- 정지 기간은 `1일 → 4일 → 7일 → 14일 → 30일 → 60일`로 증가하며 최대 60일을 유지한다.
- 이미지는 JPEG·PNG·WebP, 최대 5MB만 허용한다.

### 16.5 커뮤니티

- 완료된 여행 일정을 여행 지역별 공개 대상으로 등록한다.
- 사용자는 좋아요를 등록하거나 취소할 수 있다.
- 현재 요구사항 문서의 랭킹 공식은 시간 경과를 반영한다.

```text
(좋아요 × 3 + ln(조회수)) / (게시물 경과 시간 + 2)^1.2
```

이는 Figma에서 확인한 단순 점수 `좋아요 × 3 + 조회수 × 1`보다 구체화된 후속 정책이다.

### 16.6 포토 미션

- 개인형과 그룹형 완료 상태를 구분한다.
- AI 일치도는 `80% 이상 성공`, `40% 이상 80% 미만 애매`, `40% 미만 실패`로 판단한다.
- 정상 완료된 애매·실패 판정의 재시도는 사용자와 미션 조합별로 누적하며 날짜 변경, 삭제 또는 재제출로 초기화하지 않는다. 네트워크·외부 시스템·서버 오류로 판정 결과를 만들지 못한 동작은 횟수에 포함하지 않는다.
- 재시도가 3회를 초과하면 수동 완료를 허용하고 `manual` 방식으로 기록한다.
- 사용자는 자신이 제출한 사진만 삭제할 수 있다.
- 회원 탈퇴로 마지막 그룹 사진이 삭제되면 해당 그룹 미션을 미완료로 되돌린다.
- 외부 공유 데이터에는 여행 멤버 이름을 포함하지 않는다.

### 16.7 알림

- 여행 D-1 오전 10시에 여행 지역과 전체 여행 기간의 날씨를 한 번 조회한다.
- `weather_condition=NORMAL`이면 일반 D-1 알림, 특이 기상이면 해당 날씨 점검 알림, 조회가 실패하면 날씨 조회 실패 알림을 생성한다.
- 동일 여행·사용자·알림 유형을 중복 생성하지 않는다.
- 알림은 현재 로그인 사용자에게 생성된 것만 최신순으로 제공한다.
- 개별 읽음과 모두 읽음을 지원한다.

## 17. 확정된 비기능 요구사항의 핵심

### 데이터 정합성과 동시성

- 설문 수정 실패 시 이전 제출 결과를 보존한다.
- 여행방 참여, 설문 제출과 사진 제출 같은 상태 변경은 원자적으로 처리한다.
- 좋아요, 조회수와 설문 제출 수는 중복 요청으로 잘못 증가하면 안 된다.
- 사진 삭제와 회원 탈퇴로 사진이 제거될 때 앨범 집계와 미션 완료 상태를 함께 갱신한다.
- 재시도 가능한 요청에는 멱등성을 보장할 수 있는 식별값을 사용한다.

### 보안과 권한

- 인증이 필요한 기능은 로그인 사용자만 이용할 수 있다.
- 여행방 정보와 설문은 해당 여행방 멤버만 조회한다.
- 방장 전용 기능은 화면이 아니라 서버에서 권한을 검증한다.
- 비밀번호, 토큰과 외부 API 키를 로그나 응답에 노출하지 않는다.
- Refresh Token 회전과 재사용 탐지는 사용하지 않으며, 로그아웃과 회원 탈퇴 시 `revoked_at`을 기록해 폐기한다.
- 본인이 제출한 포토 미션 사진만 삭제할 수 있다.

### 장애 대응과 가용성

- 외부 AI·날씨·장소 검색 장애가 전체 서비스 장애로 확산되지 않게 한다.
- 날씨 조회 실패 시 실제 날씨를 임의로 추정하지 않는다.
- AI 일정 생성 실패 시 기존 여행 정보와 설문 결과를 보존한다.
- 새로고침이나 재접속 후에도 진행 중인 AI 작업을 다시 조회할 수 있게 한다.

### 확장성과 운영

- 긴 AI 작업은 비동기로 실행하고 독립된 작업 서버로 확장할 수 있게 한다.
- 이미지 데이터는 애플리케이션 서버와 독립적인 객체 저장소로 확장할 수 있게 한다.
- OAuth 실패, AI 작업 상태, 외부 API 실패, 오픈 채팅 위반 및 제재 이력을 추적한다.
- 메인 여행방, 장소 검색, 채팅 메시지, 커뮤니티, 포토 미션 지난 여행·앨범 사진과 알림 목록은 확정된 조회 개수로 cursor 페이지네이션하고 나머지 목록은 즉시 로드한다.
- 정량 성능 목표는 예상 사용자 수, 실제 부하 테스트와 운영 비용을 근거로 별도 확정한다.

## 18. 여전히 별도 결정이 필요한 항목

후속 요구사항 정제로 기능 정책은 구체화됐다. 현재 별도 결정이 필요한 내용은 API 응답시간, 채팅 지연시간, 동시 사용자와 가용성의 정량 목표 및 측정 근거다.
