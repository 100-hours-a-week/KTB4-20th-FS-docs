# 플랜잇(PlanIt) 프로젝트 개요

> **참고:** `플랜잇(PlanIt)`은 현재 사용 중인 가칭이며, 서비스명은 추후 변경될 예정입니다.

## 서비스 소개

플랜잇은 친구나 동료 등 여러 명이 함께 떠나는 국내 여행의 일정을 관리하는 그룹 여행 일정 관리 서비스입니다.

그룹 여행을 계획할 때는 구성원마다 선호하는 여행 방식과 방문하고 싶은 장소가 달라 일정 조율에 많은 시간이 들고, 합의한 장소를 단순히 나열하다 보면 이동 동선이 비효율적으로 구성되기 쉽습니다. 플랜잇은 이러한 문제를 해결하기 위해 그룹 구성원이 입력한 취향과 희망 장소를 AI가 종합하여, 그룹 전체에 적합한 하나의 여행 일정을 자동으로 생성합니다.

## 해결하려는 문제

- 그룹 구성원마다 서로 다른 여행 취향과 희망 장소로 인해 일정 합의가 어렵습니다.
- 여러 사람의 의견을 사람이 직접 취합하고 조율하는 과정에 많은 시간과 노력이 필요합니다.
- 방문 장소 간 거리와 이동 시간을 충분히 고려하지 못해 비효율적인 동선이 만들어질 수 있습니다.

## 주요 사용자

- 친구 또는 동료와 함께 국내 여행을 계획하는 그룹 여행객
- 구성원의 다양한 선호를 반영하면서 효율적인 여행 일정을 만들고 싶은 사용자

## 핵심 기능

1. 그룹 구성원별 여행 취향 입력
   - 모든 5점 척도 문항은 중립값 `3`으로 시작하며 최종 선택한 `1~5` 값을 가공하지 않고 저장합니다.
   - 그룹 설문 결과도 퍼센트로 환산하지 않고 제출 완료된 활성 멤버의 원본 점수를 카테고리별 `1.0~5.0` 평균으로 표시합니다.
   - 별도 다중 선택한 제외 카테고리와 Google Places 검색으로 저장한 꼭 가고 싶은 장소는 AI 일정 생성 시작 시 Spring 서버가 스냅샷으로 고정해 FastAPI 서버에 전달합니다.
   - 장소 추천, 포함·제외 판단과 경로 구성은 FastAPI가 전담하며 Spring 백엔드는 입력 전달, 작업 상태와 결과 저장만 담당합니다.
   - 동일 AI 작업의 자동 재시도는 최초 설문 스냅샷을 유지합니다. 최종 실패 후 방장이 다시 요청하면 최신 설문으로 새 작업을 생성하고 이전 실패 이력은 보존하되 사용자 횟수는 차감하지 않습니다.
   - 방장은 일정 확정 전에 성공 결과 기준 최대 3회까지 일정을 재생성할 수 있습니다. 일정이 한 번 확정되면 일반 재생성은 할 수 없고 장소 추가·삭제와 날씨 대체 일정만 허용됩니다.
   - 다른 후보 재확정과 확정 일정의 장소 추가·삭제는 여행 시작 전날까지만 가능합니다. 장소는 Day의 처음, 두 장소 사이 또는 마지막에 추가할 수 있으며 별도 순서 변경과 Day 간 이동은 제공하지 않습니다.
   - AI 일정은 장소 좌표 간 거리의 합을 기준으로 경로를 구성하며 이동수단과 소요 시간은 고려하지 않습니다. 음식점·카페 연속 배치와 음식점·카페가 번갈아 세 곳 이어지는 배치는 허용하지 않습니다.
   - 장소 추가·삭제 시 AI가 방문 순서를 다시 구성하지 않습니다. 백엔드는 변경된 인접 장소의 좌표 간 거리만 갱신하며, 사용자의 수동 편집에는 음식점·카페 배치 제약을 적용하지 않습니다.
   - 날씨 변경 요청에 응답하지 않으면 여행 시작 시 기존 일정을 자동 유지합니다. 방장이 변경을 선택하면 대체 일정 생성 성공 시 별도의 재확인 없이 자동 반영하며, 실패하면 기존 일정을 유지합니다.
   - 날씨 `KEEP` 결정은 변경할 수 없습니다. 대체 일정 생성이 실패하면 횟수 차감 없이 여행 시작 전까지 다시 요청할 수 있고, 성공하면 추가 날씨 재생성은 할 수 없습니다. 여행 시작 후 완료된 대체 작업은 새 일정을 저장·적용하지 않고 `FAILED`와 내부 `WEATHER_REPLAN_EXPIRED`로 종료하며 기존 확정 일정을 유지합니다.
2. 각 구성원이 방문하고 싶은 희망 장소 입력
   - 장소 검색 시 서버가 선택한 광역 지역명을 사용자 검색어 앞에 내부적으로 결합해 Google Places API를 호출합니다. 이 방식은 지역 관련도를 높이지만 행정구역 경계를 강제하지는 않습니다.
   - 검색 결과 전체를 저장하지 않고 사용자가 클릭한 Google Places 장소 하나만 저장하거나 기존 장소를 조회해 내부 장소 ID를 반환합니다. 별도 후보 Token은 사용하지 않습니다.
   - 희망 장소는 사용자별·여행별 최대 10개까지 선택할 수 있고, 선택하지 않아도 설문을 제출할 수 있으며 동일 장소 중복 선택은 허용하지 않습니다.
3. 그룹 전체의 취향과 희망 장소를 AI가 종합
4. 이동 동선을 고려한 최적화된 여행 경로 자동 생성
5. 시간대별 여행 일정 자동 구성

## 여행방 운영 정책

- 서비스에서 지원하는 광역·하위 여행 지역은 운영 전에 `regions` 테이블에 기준정보로 미리 등록합니다. 화면은 이 정보를 광역 지역별로 묶어 표시하고, 최종 선택값으로 해당 여행 지역 행의 PK인 `regions.id`를 사용합니다.
- 여행 지역은 필수 선택값입니다. 지역 기준정보를 조회할 수 없으면 여행방 생성 단계로 진행하지 않고 오류와 재시도를 제공합니다.
- 여행 시작일은 서울 날짜 기준 최소 내일이며, 시작일과 종료일을 포함해 최대 10일까지 설정할 수 있습니다. 설문 마감일은 오늘부터 여행 시작일 전날까지 선택하고 해당 날짜의 `23:59:59.999999`에 마감합니다.
- 여행방 생성 직후 방장 1명만 있는 상태는 다른 사용자를 초대할 수 있도록 유지합니다.
- 여행방 멤버 내보내기 기능은 제공하지 않습니다.
- 여행 시작 전, 한 번이라도 2명 이상이었던 여행방의 활성 멤버가 나가기·회원 탈퇴로 1명만 남으면 여행방을 소프트 삭제하고 남은 멤버십도 종료합니다.
- 방장이 여행방을 직접 삭제하는 기능은 제공하지 않습니다. 서울 날짜 기준 여행 시작일이 되면 여행방 나가기를 허용하지 않으며 종료된 여행방도 동일합니다.
- 회원 탈퇴는 여행 중·후에도 허용하지만, 이로 인해 인원이 감소하더라도 진행 중이거나 종료된 여행방은 자동 삭제하지 않습니다.
- 여행 시작 전에 나가기·회원 탈퇴로 여행방이 자동 삭제되면 확정·후보 일정과 그 하위 Day·장소·이동 구간을 함께 정리합니다. 여행방은 소프트 삭제되므로 이는 DB FK의 물리 삭제 cascade가 아니라 애플리케이션에서 수행하는 연쇄 정리입니다.
- 포토 미션 사진은 여행 시작 전에는 제출할 수 없습니다. 따라서 여행방과 일정의 연쇄 정리가 허용되는 시점에는 제출 사진이 존재하지 않습니다.
- 잘못된 초대 링크와 삭제된 여행방의 초대 링크로 접근하면 각각 관련 오류 화면을 표시합니다.
- 여행방마다 만료되지 않는 고정 초대 토큰 원문 하나를 저장합니다. 현재 방장만 같은 토큰을 다시 조회·공유할 수 있고, 클라이언트는 현재 서비스 주소와 토큰을 조합해 동일한 초대 링크를 만듭니다. 참여 화면의 초대한 사용자는 현재 방장이므로 방장 위임 후에는 새 방장을 표시합니다.
- 비로그인 사용자가 초대 링크에 접근하면 초대 정보를 공개하지 않고 로그인 화면으로 이동합니다. 로그인 또는 최초 회원 정보 생성이 끝나면 원래 초대 페이지로 복귀합니다.
- 여행이 시작됐거나, 설문 참여가 종료됐거나, 정원이 가득 찼거나, 이미 참여 중이면 초대 참여를 허용하지 않습니다.
- 신규 멤버의 초대 참여는 설문 마감 시각이 지났거나 AI 일정 생성 작업이 시작되면 종료합니다. 기존 멤버 전원이 설문을 제출한 것만으로는 종료하지 않습니다.
- 기존 활성 멤버는 AI 일정 생성 중에도 설문을 재제출할 수 있습니다. 재제출 결과는 최신 설문 집계에 반영되지만 이미 실행 중인 AI 작업과 생성 결과에는 영향을 주지 않습니다.
- 최초 설문 제출은 설문 마감 시각까지, 제출 완료자의 재제출은 서울 날짜 기준 여행 시작 전까지 허용합니다. 마감 전에 제출하지 않은 사용자는 마감 후 새로 제출할 수 없습니다.
- 사용자는 날짜가 하루라도 겹치는 다른 여행방을 생성하거나 참여할 수 없습니다.
- 여행방 이름·지역·기간·정원·설문 마감 시각은 생성 후 수정할 수 없습니다. 설문 재제출과 확정 일정의 장소 편집은 여행방 기본 정보 수정과 별개의 기능입니다.
- 삭제 시 이미 진행 중인 AI 일정 생성 작업은 취소하지 못하지만, 완료 결과를 삭제된 여행방에 반영하지 않습니다.

## 차별점

트리플과 같은 기존 여행 일정 관리 앱이 사용자가 직접 장소와 일정을 선택하고 조율하는 방식에 가깝다면, 플랜잇은 AI가 그룹 구성원 전체의 선호와 희망 장소를 종합하여 하나의 최적 동선과 시간대별 일정을 자동으로 만들어준다는 차별점이 있습니다.

이를 통해 사용자는 복잡한 의견 조율과 동선 설계에 드는 수고를 줄이고, 그룹 구성원의 취향이 균형 있게 반영된 효율적인 여행 계획을 빠르게 완성할 수 있습니다.
